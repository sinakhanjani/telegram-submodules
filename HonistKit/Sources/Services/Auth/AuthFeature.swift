import Foundation
import HonistRouting
import UIKit

public enum AuthFeature: HonistRoutable {
    // For now we have no UI. You can wire a stub controller if needed later.
    public static func makeRoot() -> UIViewController {
        return UIViewController() // Placeholder; will be replaced by real UI later.
    }
}
